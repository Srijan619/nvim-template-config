return {
  -- Other plugins...

  -- Add render-markdown.nvim plugin
  {
    "dhruvasagar/render-markdown.nvim",
    config = function()
      -- Optional: Set up any specific configuration for the plugin here
    end,
  },

  -- Other plugins...
}
